#include <QApplication>

int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    int status = 0;
    //{TestNetWorkManager tc; status |= QTest::qExec(&tc, argc, argv);}

    return status;
}
